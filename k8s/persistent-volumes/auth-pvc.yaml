apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-pvc
  namespace: microservices
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard

