apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: product-pvc
  namespace: microservices
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard

