apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: microservices
  labels:
    app: tenant-service
spec:
  type: ClusterIP
  ports:
    - port: 7000
      targetPort: 7000
      protocol: TCP
  selector:
    app: tenant-service

